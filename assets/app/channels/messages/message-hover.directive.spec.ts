import { MessageHoverDirective } from './message-hover.directive';

describe('MessageHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageHoverDirective();
    expect(directive).toBeTruthy();
  });
});
